# Server
PORT=4000
HOST=0.0.0.0
# Comma-separated list of allowed HTTP origins. Leave blank to allow all (development/mobile apps).
CORS_ALLOWED_ORIGINS=
# Optional Socket.IO override. Inherit from HTTP origins when left blank.
SOCKET_ALLOWED_ORIGINS=
# Rate limiting (defaults mirror production-safe values).
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Database & auth
MONGODB_URI=
JWT_SECRET=please_change_me
OTP_EXP_MIN=10
ADMIN_SIGNUP_TOKEN=
MONGOOSE_DEBUG=0

# SMS (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
